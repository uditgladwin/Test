import { Directive } from '@angular/core';

@Directive({
  selector: '[clariusScrollSync]',
})
export class ScrollSyncDirective {
  // Skeleton directive for synchronizing horizontal scroll between header and body.
}
